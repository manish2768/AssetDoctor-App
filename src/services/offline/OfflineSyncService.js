/**
 * Offline sync — thin re-export of SyncEngine (STEP 8).
 * Kept so existing imports continue to work without a duplicate system.
 */

export { SyncEngine, OfflineSyncService, SyncEngine as default } from './SyncEngine';
